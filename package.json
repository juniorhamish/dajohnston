{
  "name": "dajohnston",
  "version": "1.0.0",
  "description": "A scalable, low-cost, and secure system designed to host multiple independent applications (e.g., Spice Tracker, Checklistz, Housework Tracker) with shared identity and data-sharing capabilities.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.1"
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,jsx,tsx,json}": [
      "npx biome check --write --no-errors-on-unmatched"
    ],
    "backend/**/*.{java,gradle}": [
      "npx lint-staged --config backend/package.json"
    ]
  }
}
